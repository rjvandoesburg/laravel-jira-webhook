language: php
php:
  - '5.6'
  - '7.0'
  - '7.1'
  - hhvm
  - nightly


sudo: false

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer install --dev --prefer-source --no-interaction